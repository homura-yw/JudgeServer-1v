source: local

OSS:
  endpoint: secret
  accessKeyID: secret
  accessKeySecret: secret
  bucketName: secret

datapath: /root/data

rpcUrl: localhost:303

redis: 
  url: secret
  password: secret
  db: 0

groupSize: 3